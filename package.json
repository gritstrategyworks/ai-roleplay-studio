{
  "name": "ai-roleplay-studio",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --keep-vars",
    "deploy:dry": "wrangler deploy --dry-run --keep-vars",
    "check": "node --check public/app.js && node --check public/auth.js && node --check public/scenario-design.js && node --check src/index.js",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.121.0"
  }
}
